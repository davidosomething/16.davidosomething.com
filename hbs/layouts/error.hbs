{{!
	Error page
}}
{{> globalStart }}
<article class="article">
	<header class="article__header">
		<h1 class="article__title">{{{ title }}}</h1>
		{{#if subheader}}<p class="article__subheader">{{{ subheader }}}</p>{{/if}}
	</header>
	<div class="article__body">{{{ contents }}}</div>
</article>
{{> globalEnd }}
